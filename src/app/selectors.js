// selectors
export const selectUser = (state) => state.user.user;

// selectors
export const selectProf = (state) => state.prof.prof;

// selectors
export const selectProfs = (state) => state.profs;

// selectors
export const selectNewSigning = (state) => state.newSigning;

// selectors
export const selectEnAttente = (state) => state.enAttente;

// selectors
export const selectRDV = (state) => state.rdv;

// selectors
export const selectMessages = (state) => state.messages;